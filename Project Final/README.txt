This only works with firefox as other browsers cannot access text files
There are a lot of loops running when the website loads so if something doesn't seem right it probably requires reloading or refreshing
Depending on monitor size, the padding will change from the main table
